{
  "name": "pronto-config",
  "version": "0.0.1",
  "description": "Expose a JSON object in `app.locals.config`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
